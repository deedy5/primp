[package]
name = "rustls-test"
version = "0.1.0"
edition = "2021"
rust-version = "1.84"
publish = false

[dependencies]
rustls = { path = "../rustls", default-features = false, features = ["std", "tls12"] }
